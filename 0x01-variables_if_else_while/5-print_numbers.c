#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - this program  that prints all single digit numbers
 * Return: always 0
 */
int main(void)
{
int n;
for (n = '0'; n < '10'; n++)
{
putchar(a + '0');
}
putchar('\n');
return (0);
}

